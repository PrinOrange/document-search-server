import { Language } from "@/types/token.type";

export const stemmerToken = () => {};

export const removeStopwords = () => {};

export const recognizeLanguage = (): Language => {
	return Language.UNKNOWN;
};
